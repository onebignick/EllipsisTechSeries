export interface alertPopUpParams {
    itemCategory: string;
    itemName: string;
    location: string;
    datetime: string;
}

export interface officerCardParams {
    name: string;
    location: string;
    shiftDuration: string;
}